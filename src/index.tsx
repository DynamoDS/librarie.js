import * as React from "react";
import * as ReactDOM from "react-dom";

import { LibraryContainer } from "./components/LibraryContainer";

ReactDOM.render(
    <LibraryContainer />,
    document.getElementById("libraryContainerPlaceholder")
);
